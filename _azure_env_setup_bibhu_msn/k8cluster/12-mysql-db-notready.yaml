apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: mysql
spec:
  selector:
    matchLabels:
      app: mysql-pod
  serviceName: mysql-h-svc
  replicas: 3
  template:
    metadata:
      labels:
        app: mysql-pod
    spec:
      containers:
      - name: mysql-pod
        image: mysql
---
